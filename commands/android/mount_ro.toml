description = "Remount the system partition in read-only mode."
prompt = """
Remount the system partition in read-only mode.
Enter the following parameters: <partition_path param name>.
Example execution: adb shell mount -o ro,remount /system.
"""
