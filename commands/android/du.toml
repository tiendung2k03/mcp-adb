description = "Display the disk usage of a file or directory."
prompt = """
Display the disk usage of a file or directory.
Enter the following parameters: <path param name>.
Example execution: adb shell du -h /sdcard.
"""
